spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskman?autoReconnect=true&useSSL=false
    username: macAdmin
    password: 253352

  jpa.hibernate.ddl_auto : update
  jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQLDialect
security:
  ignored: /**